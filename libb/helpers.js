const sum = (x, b) => {
    return x + b;
}

const personObject = {
    age: 20,
    name: 'Sarah'
}


const stringToUpperCase = (string) => { 
    return string.toUpperCase()
}


module.exports = {
    sum,
    personObject,
    stringToUpperCase
}